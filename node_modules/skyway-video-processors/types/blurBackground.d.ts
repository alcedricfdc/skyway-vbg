import { ProcessedStream } from './processedStream';
type BlurBackgroundOptions = {
    blur: number;
};
export declare class BlurBackground {
    private isInitialized;
    private readonly options;
    private readonly selfieSegmentation;
    private processCanvas;
    private processCanvasCtx;
    private blurRadius;
    private transformerAbortController;
    constructor(options?: BlurBackgroundOptions);
    private selfieSegmentationOnResults;
    initialize(): Promise<void>;
    private getProcessedMediaStreamTrack;
    createProcessedStream(options?: {
        stopTrackWhenDisabled?: boolean;
        onUpdateTrack?: (track: MediaStreamTrack) => Promise<void>;
        constraints?: MediaTrackConstraints;
    }): Promise<ProcessedStream>;
    dispose(): Promise<void>;
}
export {};
//# sourceMappingURL=blurBackground.d.ts.map